{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "migrate",
            "type": "shell",
            "command": "make migrate",
            "options": {
                "env": {
                    "DB_URI": "postgres://postgres:password@127.0.0.1:5432/dl_tests"
                },
            },
            "presentation": {
                "reveal": "silent"
            }
        }
    ]
}
